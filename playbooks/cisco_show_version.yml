---
- name: Test Cisco IOS connectivity
  hosts: cisco_ios
  gather_facts: false

  tasks:
    - name: Run show version
      cisco.ios.ios_command:
        commands:
          - show version
      register: show_version

    - name: Display show version output
      ansible.builtin.debug:
        var: show_version.stdout_lines
